{
  "name": "chat-assistant",
  "version": "1.0.0",
  "description": "Chat Assistant application with Angular frontend and .NET backend",
  "scripts": {
    "start": "cd angular && npm start",
    "build": "cd angular && npm run build",
    "test": "cd angular && npm test"
  },
  "private": true
}
